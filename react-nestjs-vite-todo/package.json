{
    "name": "react-nestjs-vite-todo",
    "private": true,
    "version": "1.0.0",
    "workspaces": [
      "apps/*"
    ],
    "scripts": {
      "build": "pnpm  --parallel --filter \"./**\" build",
      "dev": "pnpm  --parallel --filter \"./**\" dev",
      "stop": "kill-port --port 5000,5001"
    },
    "devDependencies": {
      "kill-port": "^2.0.1"
    }
  }
  